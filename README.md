# BankingAutomation
Built an automated data pipeline to ingest and process transactional data, detect anomalies, and serve analytics via APIs and a dashboard. Uses PostgreSQL for storage and FastAPI for data access, with ingestion state tracking and time-based analysis.
